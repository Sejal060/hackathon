# src/input_handler.py
class InputHandler:
    def process_input(self, user_input: str) -> str:
        # Simple preprocessing (expand as needed)
        processed = user_input.strip().lower()
        return processed
